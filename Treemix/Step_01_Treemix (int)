# Get an interactive session. 

# Enter the 07_Treemix folder. 

# Make a .clust file. 

# Import the vcf2treemix.sh script from here: https://github.com/speciationgenomics/scripts/blob/master/vcf2treemix.sh 
# Or... just use the version that I modified. That would actually be better. 

# Download the plink2treemix.py script and put it somewhere.  

# Merge chromosome-level vcfs. Starting with the thinned dataset. 
gunzip *Treemix_50kbpthin.recode.vcf.gz
grep "#" CM019901.1__Treemix_50kbpthin.recode.vcf > TM_50kbpthin.vcf
for i in $( ls *Treemix_50kbpthin.recode.vcf ); do grep -v "#" $i >> TM_50kbpthin.vcf; done
gzip *.vcf

# Then this:
bash vcf2treemix.sh TM_50kbpthin.vcf.gz birbs.clust

